import React, { useEffect } from 'react';
import Header from '../../components/Header';

interface CustomerSupportTicketProps {}

const CustomerSupportTicket = (props: CustomerSupportTicketProps) => {
  return (
    <div>
      <Header title='Customer Support Ticket' />
    </div>
  );
};

export default CustomerSupportTicket;
